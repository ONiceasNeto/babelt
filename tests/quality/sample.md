# Amostra de traduções — fase 3

Gerado por `tests/quality/measure_translate.py`. Serve para inspeção
manual: a validação garante que a sintaxe sobreviveu, não que o
português esteja bom.

## 1. **mantido em inglês** — 1 de 1 sentenças rejeitadas

**EN**

```
MAWK(1)                         User commands                        MAWK(1)
```

**pt-BR**

```
MAWK(1)                         User commands                        MAWK(1)
```

## 2. traduzido

**EN**

```
Omit features such as interval expressions which were not supported by traditional awk.
```

**pt-BR**

```
Omit características como expressões de intervalo que não foram suportados pelo awk tradicional.
```

## 3. traduzido

**EN**

```
chmod [OPTION]... MODE[,MODE]... FILE...
chmod [OPTION]... OCTAL-MODE FILE...
chmod [OPTION]... --reference=RFILE FILE...
```

**pt-BR**

```
[Opção]... MODE[,MODE]... Imagem...
[Opção]... OCTAL-MODE FILE...
[Opção]... --reference=RFILE Imagem...
```

## 4. traduzido

**EN**

```
curl [options / URLs]
```

**pt-BR**

```
[opções / URLs]
```

## 5. traduzido

**EN**

```
DICT Lets you lookup words using online dictionaries.
```

**pt-BR**

```
DICTO Permite pesquisar palavras usando dicionários online.
```

## 6. **mantido em inglês** — 1 de 1 sentenças rejeitadas

**EN**

```
The LEVEL of information to print to stderr; 'none' suppresses
everything but error messages, 'noxfer' suppresses the final
transfer statistics, 'progress' shows periodic transfer statistics
```

**pt-BR**

```
The LEVEL of information to print to stderr; 'none' suppresses
everything but error messages, 'noxfer' suppresses the final
transfer statistics, 'progress' shows periodic transfer statistics
```

## 7. traduzido

**EN**

```
append append mode (makes sense only for output; conv=notrunc suggested)
```

**pt-BR**

```
modo de apêndice append (faz sentido apenas para saída; conv=notrunc sugerido)
```

## 8. **mantido em inglês** — 1 de 1 sentenças rejeitadas

**EN**

```
find [-H] [-L] [-P] [-D debugopts] [-Olevel] [starting-point...] [expression]
```

**pt-BR**

```
find [-H] [-L] [-P] [-D debugopts] [-Olevel] [starting-point...] [expression]
```

## 9. traduzido

**EN**

```
help Explain the debugging options.
```

**pt-BR**

```
ajudar a explicar as opções de depuração.
```

## 10. **mantido em inglês** — oov

**EN**

```
• number
```

**pt-BR**

```
• number
```

## 11. traduzido

**EN**

```
Suppress normal output; instead print a count of matching
lines for each input file. With the -v, --invert-match option
(see above), count non-matching lines.
```

**pt-BR**

```
Suprimir a saída normal; em vez imprimir uma contagem de linhas correspondentes para cada arquivo de entrada. Com a opção -v, --invert-match (veja acima), conte linhas não correspondentes.
```

## 12. **mantido em inglês** — 1 de 1 sentenças rejeitadas

**EN**

```
-B     shortcut for -family bridge.
```

**pt-BR**

```
-B     shortcut for -family bridge.
```

## 13. traduzido

**EN**

```
l2tp - tunnel ethernet over IP (L2TPv3).
```

**pt-BR**

```
l2tp - túnel ethernet sobre IP (L2TPv3).
```

## 14. traduzido

**EN**

```
There are currently five independent tables (which tables are present
at any time depends on the kernel configuration options and which
modules are present).
```

**pt-BR**

```
Atualmente existem cinco tabelas independentes (quais tabelas estão presentes a qualquer momento depende das opções de configuração do kernel e quais módulos estão presentes).
```

## 15. traduzido

**EN**

```
If called without parameters, it will show the contents of the
journal accessible to the calling user, starting with the oldest
entry collected.
```

**pt-BR**

```
Se chamado sem parâmetros, ele mostrará o conteúdo da revista acessível ao usuário que está chamando, começando com a entrada mais antiga coletada.
```

## 16. traduzido

**EN**

```
Takes an image policy string as argument, as per systemd.image-policy(7). The policy is enforced when operating on the disk
image specified via --image=, see above. If not specified
```

**pt-BR**

```
Toma uma cadeia de política de imagem como argumento, conforme systemd.image-política(7). A política é aplicada quando se opera na imagem do disco especificada via --image=, veja acima. Se não especificado
```

## 17. traduzido

**EN**

```
Output info about virtio devices only.
```

**pt-BR**

```
Informações de saída apenas sobre dispositivos virtio.
```

## 18. traduzido

**EN**

```
The following command lists all mounted filesystems (of type type):
```

**pt-BR**

```
O comando a seguir lista todos os sistemas de arquivos montados (do tipo):
```

## 19. traduzido

**EN**

```
In addition to the interesting ports table, Nmap can provide further
information on targets, including reverse DNS names, operating system
guesses, device types, and MAC addresses.
```

**pt-BR**

```
Além da interessante tabela de portas, o Nmap pode fornecer mais informações sobre alvos, incluindo nomes DNS reversos, suposições do sistema operacional, tipos de dispositivos e endereços MAC.
```

## 20. traduzido

**EN**

```
To see every process on the system using standard syntax:
```

**pt-BR**

```
Para ver todos os processos no sistema usando sintaxe padrão:
```

## 21. traduzido

**EN**

```
conditions (negates the selection). Identical to --deselect.
```

**pt-BR**

```
(refere-se à seleção). Idêntico a --deselect.
```

## 22. traduzido

**EN**

```
Usages with just one SRC arg and no DEST arg will list the source
files instead of copying.
```

**pt-BR**

```
Usos com apenas um SRC arg e nenhum DEST arg listarão os arquivos de origem em vez de copiar.
```

## 23. traduzido

**EN**

```
A trailing slash on the source changes this behavior to avoid creating an additional directory level at the destination. You can think
of a trailing / on a source as meaning "copy the contents of this directory" as opposed to "copy the directory by name", but in both
cases the attributes of the containing directory are transferred to
the containing directory on the destination. In other words, each of
the following commands copies the files in the same way, including
their setting of the attributes of /dest/foo:
```

**pt-BR**

```
Uma barra de trailing na fonte muda esse comportamento para evitar a criação de um nível de diretório adicional no destino. Você pode pensar em uma fuga / em uma fonte como significando "copiar o conteúdo deste diretório" em oposição a "copiar o diretório pelo nome", mas em ambos os casos os atributos do diretório contendo são transferidos para o diretório contendo no destino. Em outras palavras, cada um dos seguintes comandos copia os arquivos da mesma maneira, incluindo a configuração dos atributos de /dest/foo:
```

## 24. traduzido

**EN**

```
#comment
```

**pt-BR**

```
#comment
```

## 25. traduzido

**EN**

```
D      If pattern space contains no newline, start a normal new cycle
```

**pt-BR**

```
D Se o espaço padrão não contiver nenhuma nova linha, inicie um novo ciclo normal
```

## 26. traduzido

**EN**

```
Show socket memory usage. The output format is:
```

**pt-BR**

```
Mostrar o uso da memória do socket. O formato de saída é:
```

## 27. traduzido

**EN**

```
ssh [-Q query_option]
```

**pt-BR**

```
ssh [-Q query_option]
```

## 28. traduzido — 1 de 4 sentenças rejeitadas

**EN**

```
Specifies an alternative per-user configuration file. If a
configuration file is given on the command line, the system-wide configuration file (/etc/ssh/ssh_config) will be ignored. The default for the per-user configuration file is
~/.ssh/config. If set to “none”, no configuration files will
be read.
```

**pt-BR**

```
Especifica um arquivo de configuração alternativo por usuário. If a
configuration file is given on the command line, the system-wide configuration file (/etc/ssh/ssh_config) will be ignored. O padrão para o arquivo de configuração por usuário é ~/.ssh/config. Se definido como “none”, nenhum arquivo de configuração será lido.
```

## 29. traduzido

**EN**

```
command may be used to display the current set of possible
values.
```

**pt-BR**

```
O comando pode ser usado para exibir o conjunto atual de valores possíveis.
```

## 30. traduzido

**EN**

```
tar - an archiving utility
```

**pt-BR**

```
tar - um utilitário de arquivamento
```
